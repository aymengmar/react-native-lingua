diff --git a/node_modules/react-native-css/dist/commonjs/metro/index.js b/node_modules/react-native-css/dist/commonjs/metro/index.js
index 998b303..4145192 100644
--- a/node_modules/react-native-css/dist/commonjs/metro/index.js
+++ b/node_modules/react-native-css/dist/commonjs/metro/index.js
@@ -127,17 +127,19 @@ function withReactNativeCSS(config, options) {
                   nativeCSSFiles.set(filePath, [next, (0, _compiler.compile)(next, {
                     hexColors: options?.hexColors
                   }).stylesheet()]);
+                  // Metro 0.84+ expects { changes: { addedFiles, modifiedFiles, removedFiles }, rootDir }
+                  // instead of the legacy { eventsQueue } format.
                   watcher.emit("change", {
-                    eventsQueue: nativeInjectionFilepaths.map(filePath => ({
-                      filePath,
-                      metadata: {
+                    changes: {
+                      addedFiles: new Map(),
+                      modifiedFiles: new Map(nativeInjectionFilepaths.map(fp => [fp, {
                         modifiedTime: Date.now(),
                         size: 1,
-                        // Can be anything
-                        type: "virtual" // Can be anything
-                      },
-                      type: "change"
-                    }))
+                        type: "virtual"
+                      }])),
+                      removedFiles: new Map()
+                    },
+                    rootDir: ""
                   });
                 }
                 const nativeTransform = await transformFile(filePath, transformOptions, fileBuffer);
